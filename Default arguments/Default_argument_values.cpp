#include <iostream>
#include <string>

using namespace std;

int main(void){
	
	/*
		double calc_cost(double base_cost, double tax_rate = 0.06);
		
		double calc_cost(double base_cost, double tax_rate)
		{
			return base_cost+=(base_cost*tax_rate);
		}
		
		int main(void){
			double cost{0};
			cost=calc_cost(200.0); // since tax rate is not indicated it will use default value for the tax_rate
			cost=calc_cos(100,0.08); //will use 0.08 instead of default value		
		}
		
	*/
}