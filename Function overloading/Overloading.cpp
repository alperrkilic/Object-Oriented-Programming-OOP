#include <iostream>

using namespace std;

int main(void){
	
	/*
		void display(int n);
		void display(double d);
		void display(std::string s);
		void display(std::string s,std::string t);
		void display(std::vector<int> v);
		void display(std::vector<std::string> v);
		
		If I call display function the compiler will check
		which parameter do I use to call the function
		and match it. This is called overloading functions	
	
		However;
		
		int get_value();
		double get_value();
		
		for this example compiler would produce an error
		since the only difference is the return type
		
		cout<<get_value()<<endl;
		for that case compiler would be confused to call
		which of these functions and therefore an error will occur.
	*/
		
	
}